<div id="backgroundprueba"></div>
<div id='calendar' style="background-color: white;"></div>

@section prueba_script {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var calendarEl = document.getElementById('calendar');

            var today = new Date();
            var dd = String(today.getDate()).padStart(2, '0');
            var mm = String(today.getMonth() + 1).padStart(2, '0'); //January is 0!
            var yyyy = today.getFullYear();

            today = yyyy + "-" + mm + "-" + dd;

            var calendar = new FullCalendar.Calendar(calendarEl, {
                headerToolbar: {
                    left: 'prev,next today',
                    center: 'title',
                    right: 'dayGridMonth,timeGridWeek,timeGridDay'
                },
                buttonText: {
                    today: 'Hoy',
                    day: 'Día',
                    week: 'Semana',
                    month: 'Mes'
                },
                locale: 'es',
                firstDay: 1,
                slotDuration: '00:10:00',
                snapDuration: '00:05:00',
                initialDate: today,
                nowIndicator: 'true',
                allDaySlot: false,
                contentHeight: "auto",
                navLinks: true, // can click day/week names to navigate views
                selectable: true,
                selectMirror: true,
                businessHours: {
                    daysOfWeek: [1, 2, 3, 4, 5],
                    startTime: '08:00',
                    endTime: '21:00',
                },
                select: function (arg) {
                    var title = prompt('Event Title:');
                    if (title) {
                        calendar.addEvent({
                            title: title,
                            start: arg.start,
                            end: arg.end,
                            allDay: arg.allDay
                        })
                    }
                    calendar.unselect()
                },
                eventClick: function (arg) {
                    console.log(arg.event.extendedProps);
                    // if (confirm('Are you sure you want to delete this event?')) {
                    //   arg.event.remove()
                    // }
                },
                editable: true,
                dayMaxEvents: true, // allow "more" link when too many events
                events: [
                    {
                        title: 'All Day Event',
                        start: '2023-03-15'
                    },
                    {
                        title: 'Long Event',
                        start: '2023-03-15',
                        end: '2023-03-28'
                    },
                    {
                        groupId: 999,
                        title: 'Repeating Event',
                        start: '2023-03-17T16:00:00'
                    },
                    {
                        groupId: 999,
                        title: 'Repeating Event',
                        start: '2023-03-18T16:00:00'
                    },
                    {
                        title: 'Click for Google',
                        start: '2023-03-31T10:30:00',
                        end: '2023-03-31T11:30:00',
                    },
                    {
                        title: 'BCH237',
                        start: '2023-03-01T10:35:00',
                        end: '2023-03-02T11:30:00',
                        extendedProps: {
                            department: 'BioChemistry'
                        },
                        description: 'Lecture'
                    }
                ]
            });

            // calendar.setOption('slotDuration', '00:10:00');
            calendar.setOption('height', 500);
            calendar.render();

            var elements = document.querySelectorAll('.fc-header-toolbar [title]');
            for (var i = 0; i < elements.length; i++) {
                elements[i].removeAttribute('title');
            }

            // Una idea podría ser que creasen todos los registros que fuesen
            // Almacenarlos en extendedProps yyyy quizá revisar estos datos antes
            // de salir de la vista (¿?) y que se confirme todo con guardar
            // console.log(calendar.getEvents()[5].extendedProps);

        });

    </script>
}